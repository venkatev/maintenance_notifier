<style>
    #maintenance_alert {
        background-color: #FAFAFA;
        border: 2px solid #994433;
        color: #000000;
        padding: 5px;
        position: absolute;
        width: 500px;
    }
    
    #maintenance_alert h3 {
        color: #990000;
        margin-bottom: 3px;
    }
    
    #maintenance_alert p {
        margin: 0;
        padding: 2px;
    }
    
    #maintenance_alert .maint_time {
        font-weight: bold;
        white-space: nowrap;
    }
    
    #maintenance_alert a#close_maint {
        color: #990000;
        float: right;
        font-size: 1.2em;
        font-weight: bold;
        left: 1px;
        position: relative;
        top: -5px;
    }
</style>
<div id='maintenance_alert'>
    <%= link_to_remote 'x', :url => {:controller => 'maintenance', :action => 'hide'}, :html => {:id => 'close_maint'} %>
    <h3>Maintenance</h3>
    <p>
        The site will be down for a scheduled maintenance between <span class='maint_time'><%= @maintenance_start_time.strftime('%B %d, %Y %I:%M %p ') %></span>
        and <span class='maint_time'><%= @maintenance_end_time.strftime('%B %d, %Y %I:%M %p') %></span>
    </p>
</div>
